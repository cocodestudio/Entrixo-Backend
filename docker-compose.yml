version: '3.8'

services:
  entrixo_app:
    image: cocodestudio/entrixo-backend:v2
    restart: unless-stopped
    ports:
      - "9000:80"
    environment:
      - APP_NAME=Entrixo
      - APP_ENV=production
      - APP_KEY=base64:CztBceWpViMaDbIdTylR4oWIosNzl+gCSl7OjUk1QoE=
      - APP_DEBUG=false
      - APP_URL="http://103.57.178.66:9000"
      - DB_CONNECTION=mysql
      - DB_HOST=entrixo_db  
      - DB_PORT=3306
      - DB_DATABASE=entrixo_db
      - DB_USERNAME=root
      - DB_PASSWORD=Entrixo@123
      - MAIL_MAILER=smtp
      - MAIL_HOST=smtp.gmail.com
      - MAIL_PORT=465
      - MAIL_USERNAME=mohabuzar.net@gmail.com
      - MAIL_PASSWORD=offvkpzvuxhkdkjo
      - MAIL_ENCRYPTION=ssl
      - FIREBASE_CREDENTIALS=storage/app/firebase-auth.json
      - FILESYSTEM_DISK=public
    depends_on:
      - entrixo_db

  entrixo_db:
    image: mysql:8.0
    environment:
      - MYSQL_DATABASE=entrixo_db
      - MYSQL_ROOT_PASSWORD=Entrixo@123
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data: